# 视频功能使用指南

## 概述

本应用已集成完整的视频功能，支持视频上传、播放、点赞、评论等社交功能。视频文件暂时存储在本地服务器上。

## 功能特性

### 1. 视频管理
- **视频上传**: 支持多种视频格式（MP4, AVI, MOV, WMV, FLV, MKV, WebM）
- **视频播放**: 支持在线播放和观看统计
- **视频管理**: 支持编辑、删除、状态管理
- **缩略图**: 自动生成视频缩略图

### 2. 社交功能
- **点赞系统**: 用户可以对视频进行点赞/取消点赞
- **评论系统**: 支持多级评论和回复功能
- **观看统计**: 记录观看次数和观看时长
- **用户互动**: 基于IP的用户识别系统

### 3. 数据统计
- **观看数据**: 观看次数、观看时长、完整观看率
- **互动数据**: 点赞数、评论数、用户参与度
- **趋势分析**: 热门视频、最新视频、点赞趋势

## 数据库结构

### 主要数据表

1. **videos** - 视频主表
   - 基本信息：标题、描述、文件路径
   - 技术信息：格式、分辨率、时长、文件大小
   - 统计信息：观看数、点赞数、评论数
   - 状态管理：active/inactive/deleted

2. **video_likes** - 视频点赞表
   - 用户标识：基于IP和用户代理
   - 防重复：同一用户只能点赞一次
   - 时间记录：点赞时间戳

3. **video_comments** - 视频评论表
   - 多级评论：支持回复功能
   - 用户信息：用户昵称、IP地址
   - 状态管理：active/hidden/deleted

4. **comment_likes** - 评论点赞表
   - 评论点赞：用户可以对评论点赞
   - 防重复：同一用户只能点赞一次

5. **video_views** - 观看记录表
   - 观看统计：观看时长、是否完整观看
   - 用户追踪：IP地址、用户代理

## API接口

### 视频相关接口

#### 获取视频列表
```
GET /api/videos?page=1&limit=12&sortBy=created_at&sortOrder=DESC
```

#### 获取视频详情
```
GET /api/videos/:id
```

#### 上传视频
```
POST /api/videos/upload
Content-Type: multipart/form-data
Body: {
  title: "视频标题",
  description: "视频描述",
  video: [文件]
}
```

#### 搜索视频
```
GET /api/videos/search/:keyword?page=1&limit=12
```

#### 获取热门视频
```
GET /api/videos/popular/:limit
```

#### 获取最新视频
```
GET /api/videos/latest/:limit
```

### 点赞相关接口

#### 视频点赞/取消点赞
```
POST /api/videos/:id/like
```

#### 检查点赞状态
```
GET /api/videos/:id/like/status
```

#### 获取视频统计
```
GET /api/videos/:id/stats
```

### 评论相关接口

#### 获取评论列表
```
GET /api/videos/:id/comments?page=1&limit=20&parentId=null
```

#### 发布评论
```
POST /api/videos/:id/comments
Body: {
  content: "评论内容",
  parentId: null,  // 回复的评论ID
  userName: "用户昵称"
}
```

#### 评论点赞
```
POST /api/videos/comments/:commentId/like
```

#### 获取评论回复
```
GET /api/videos/comments/:commentId/replies?page=1&limit=10
```

## 管理界面

### 视频管理页面
访问 `/admin/videos` 可以：
- 查看所有视频列表
- 上传新视频
- 编辑视频信息
- 删除视频
- 搜索视频
- 查看视频统计

### 功能按钮
- **上传视频**: 打开上传模态框
- **编辑**: 修改视频标题、描述、状态
- **删除**: 软删除视频（标记为deleted）
- **查看**: 在新窗口打开视频详情

## 文件存储

### 目录结构
```
public/
├── uploads/
│   ├── videos/          # 视频文件存储
│   └── thumbnails/      # 缩略图存储
```

### 文件命名规则
- 视频文件：`video-{timestamp}-{random}.{ext}`
- 缩略图：`thumb-{videoId}.{ext}`

### 支持格式
- **视频**: MP4, AVI, MOV, WMV, FLV, MKV, WebM
- **图片**: JPG, PNG, GIF (缩略图)

### 文件大小限制
- 视频文件：最大500MB
- 缩略图：最大10MB

## 用户系统

### 用户识别
由于没有完整的用户系统，采用以下方式识别用户：
- **用户ID**: 基于IP地址和时间戳生成
- **IP地址**: 记录用户IP用于统计
- **用户代理**: 记录浏览器信息

### 防重复机制
- 点赞防重复：同一用户ID只能点赞一次
- 评论限制：基于IP的简单限制
- 观看统计：记录每次观看但不限制

## 性能优化

### 数据库优化
- 创建了相关索引提高查询性能
- 使用触发器自动更新统计信息
- 分页查询避免大量数据加载

### 文件处理
- 异步文件上传
- 文件格式验证
- 文件大小限制
- 唯一文件名生成

### 缓存策略
- 视频文件静态服务
- 缩略图缓存
- 数据库查询优化

## 安全考虑

### 文件上传安全
- 文件类型验证
- 文件大小限制
- 文件名安全处理
- 上传目录权限控制

### 数据安全
- SQL注入防护
- XSS攻击防护
- CSRF防护
- 输入验证和清理

## 部署说明

### 环境要求
- Node.js 14+
- SQL Server 2016+
- 足够的磁盘空间存储视频文件

### 配置步骤
1. 创建数据库表结构
2. 配置环境变量
3. 创建上传目录
4. 设置文件权限
5. 启动应用服务

### 数据库初始化
```sql
-- 执行数据库脚本
\database\create_videos_tables.sql
```

### 目录权限
```bash
# 创建上传目录
mkdir -p public/uploads/videos public/uploads/thumbnails

# 设置权限
chmod 755 public/uploads
chmod 755 public/uploads/videos
chmod 755 public/uploads/thumbnails
```

## 故障排除

### 常见问题

1. **视频上传失败**
   - 检查文件格式是否支持
   - 检查文件大小是否超限
   - 检查上传目录权限

2. **视频播放失败**
   - 检查文件路径是否正确
   - 检查文件是否存在
   - 检查MIME类型配置

3. **数据库连接问题**
   - 检查数据库连接配置
   - 检查表结构是否正确
   - 检查触发器是否创建

### 调试建议
1. 查看应用日志
2. 检查数据库连接状态
3. 验证文件上传路径
4. 测试API接口响应

## 扩展功能

### 未来计划
1. **用户系统**: 完整的用户注册登录
2. **视频处理**: 自动生成缩略图、转码
3. **CDN支持**: 视频文件CDN分发
4. **移动端**: 响应式视频播放器
5. **直播功能**: 实时视频直播
6. **推荐系统**: 基于用户行为的视频推荐

### 技术栈升级
1. **视频处理**: FFmpeg集成
2. **存储优化**: 对象存储服务
3. **缓存系统**: Redis缓存
4. **消息队列**: 异步任务处理
5. **监控系统**: 性能监控和告警

这个视频功能为应用提供了完整的视频社交体验，支持用户上传、观看、互动等核心功能，为后续功能扩展奠定了良好基础。 