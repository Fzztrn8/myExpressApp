# 数据管理后台使用指南

## 概述

为了方便组内其他成员向数据库插入数据，我们创建了一个完整的数据管理后台系统。该系统提供了直观的Web界面，支持电影和影院数据的增删改查操作。

## 访问方式

### 本地开发环境
```
http://localhost:3000/admin
```

### 生产环境
```
https://your-app-name.azurewebsites.net/admin
```

## 功能模块

### 1. 🎬 电影数据管理

#### 查看电影列表
- 访问：`/admin/movies`
- 功能：显示所有电影数据，支持编辑和删除操作

#### 添加新电影
- 访问：`/admin/movies/add`
- 必填字段：电影标题
- 可选字段：导演、演员、类型、评分、海报图片、简介

#### 编辑电影
- 访问：`/admin/movies/edit/:id`
- 功能：修改现有电影的所有信息

#### 删除电影
- 操作：在电影列表中点击"删除"按钮
- 注意：删除操作不可恢复

### 2. 🎭 影院数据管理

#### 查看影院列表
- 访问：`/admin/cinemas`
- 功能：显示所有影院数据，支持编辑和删除操作

#### 添加新影院
- 访问：`/admin/cinemas/add`
- 必填字段：影院ID、影院名称
- 可选字段：地址、城市、距离、平均票价、评分

#### 编辑影院
- 访问：`/admin/cinemas/edit/:id`
- 功能：修改现有影院的所有信息

#### 删除影院
- 操作：在影院列表中点击"删除"按钮
- 注意：删除操作不可恢复

### 3. 📥 批量数据导入

#### 批量导入电影
- 访问：`/admin/import`
- 格式：JSON数组
- 示例：
```json
[
  {
    "title": "电影标题",
    "director": "导演姓名",
    "actor": "演员姓名",
    "type": "电影类型",
    "score": 9.0,
    "image": "海报图片URL",
    "description": "电影简介"
  }
]
```

#### 批量导入影院
- 访问：`/admin/import`
- 格式：JSON数组
- 示例：
```json
[
  {
    "id": "C001",
    "name": "影院名称",
    "address": "影院地址",
    "distance_km": 2.5,
    "avg_price": 45.00,
    "rating": 8.5,
    "city": "城市名称"
  }
]
```

## 数据字段说明

### 电影数据字段

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| title | 字符串 | 是 | 电影标题 |
| director | 字符串 | 否 | 导演姓名 |
| actor | 字符串 | 否 | 演员姓名 |
| type | 字符串 | 否 | 电影类型（剧情、喜剧、动作等） |
| score | 数字 | 否 | 评分（0-10分，支持小数点） |
| image | 字符串 | 否 | 海报图片URL |
| description | 字符串 | 否 | 电影简介 |

### 影院数据字段

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | 字符串 | 是 | 影院唯一标识符 |
| name | 字符串 | 是 | 影院名称 |
| address | 字符串 | 否 | 影院地址 |
| distance_km | 数字 | 否 | 距离（公里） |
| avg_price | 数字 | 否 | 平均票价 |
| rating | 数字 | 否 | 评分（0-10分） |
| city | 字符串 | 否 | 所在城市 |

## 使用建议

### 1. 数据录入建议
- **电影标题**：使用准确的中文或英文标题
- **导演演员**：多个人员用逗号分隔
- **评分**：建议使用0-10分的评分体系
- **影院ID**：建议使用字母+数字格式，如C001、C002

### 2. 批量导入建议
- 先在本地测试JSON格式的正确性
- 分批导入大量数据，避免一次性导入过多
- 导入前备份现有数据

### 3. 数据维护建议
- 定期检查数据完整性
- 及时更新过时的信息
- 保持数据格式的一致性

## 常见问题

### Q: 如何批量导入大量数据？
A: 使用批量导入功能，将数据整理成JSON格式，然后粘贴到导入页面。

### Q: 删除的数据可以恢复吗？
A: 不可以，删除操作是永久性的，请谨慎操作。

### Q: 支持哪些图片格式？
A: 支持常见的图片格式（jpg、png、gif等），需要提供完整的URL地址。

### Q: 评分系统如何设置？
A: 使用0-10分的评分体系，支持小数点，如8.5、9.0等。

### Q: 影院ID有什么要求？
A: 影院ID必须是唯一的，建议使用字母+数字的格式，如C001、C002等。

## 技术支持

如果在使用过程中遇到问题，请：

1. 检查数据格式是否正确
2. 确认必填字段已填写
3. 查看页面提示的错误信息
4. 联系系统管理员

## 数据安全

- 所有操作都有日志记录
- 删除操作需要确认
- 建议定期备份重要数据
- 不要在公共网络环境下操作敏感数据 